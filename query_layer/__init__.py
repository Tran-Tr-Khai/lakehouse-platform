# query_layer — DuckDB-based query interface for the Gold layer on MinIO.
#
# Usage (FastAPI):
#   from query_layer.connection import get_thread_connection
#
# Usage (setup):
#   from query_layer.connection import open_catalog_for_setup
#   from query_layer.catalog import setup_views
